<% @page_title = "Sections" %>
<div class="sections index">
  <h2>Sections</h2>

  <%= link_to("Add New Section", {:action => 'new'}, :class => 'action new') %>

  <table class="listing" summary="Section list">
    <tr class="header">
      <th>&nbsp;</th>
      <th>Section</th>
      <th>Page ID</th>
      <th>Visible</th>
      <th>Content Type</th>
    </tr>
    <% @sections.each do |section| %>
    <tr>
      <td><%= section.position %></td>
      <td><%= section.name %></td>
      <td class="center"><%= section.page_id %></td>
      <td class="center"><%= section.visible ? 'Yes' : 'No' %></td>
      <td class="center"><%= section.content_type %></td>
      <td class="actions">
        <%= link_to("Show", {:action => 'show', :id => section.id}, :class => 'action show') %>
        <%= link_to("Edit", {:action => 'edit', :id => section.id}, :class => 'action show') %>
        <%= link_to("Delete", {:action => 'delete', :id => section.id}, :class => 'action show') %>
      </td>
    </tr>
    <% end %>
  </table>
</div>


